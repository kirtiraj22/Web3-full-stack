//to import Etherjs library in our file we can do the following :

//for Node.js :
const { ethers } = require("ethers");

//for ES6 or TypeScript : 
import { ethers } from "ethers";


//this should be done globally i.e outside the main function
//because we want to import all the packages before we call the main function
